<Window x:Class="TimeTracker.View.TimeTrackerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TimeTracker.View"
        mc:Ignorable="d"
        Title="TimeTrackerWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding WorkStatus}" 
                   FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"
                   Foreground="DarkBlue"/>

        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="2" 
                CornerRadius="10" Padding="20" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Отработанное время сегодня:" 
                          FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <TextBlock Text="{Binding ElapsedTimeText}" 
                          FontSize="48" FontWeight="Bold" 
                          HorizontalAlignment="Center" Foreground="Green"/>

                <TextBlock Text="часы : минуты : секунды" 
                          FontSize="12" HorizontalAlignment="Center" 
                          Foreground="Gray" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,0,0,20">
            <Button Content="НАЧАТЬ РАБОЧИЙ ДЕНЬ" 
                    Command="{Binding StartWorkDayCommand}"
                    Width="216" Height="40" Margin="10" FontWeight="Bold"/>

            <Button Content="ЗАКОНЧИТЬ РАБОЧИЙ ДЕНЬ" 
                    Command="{Binding EndWorkDayCommand}"
                    Width="252" Height="40" Margin="10" FontWeight="Bold"/>
        </StackPanel>

        <Border Grid.Row="3" Background="LightYellow" 
                CornerRadius="5" Padding="15" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Сводка за сегодня" 
                          FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                <TextBlock Text="{Binding TodaySummary}" FontSize="14"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="4" BorderBrush="LightGray" BorderThickness="1" 
                CornerRadius="5" Padding="10">
            <StackPanel>
                <TextBlock Text="Инструкция:" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock Text="1. Нажмите 'НАЧАТЬ РАБОЧИЙ ДЕНЬ' когда приходите на работу" 
                          TextWrapping="Wrap" Margin="0,2"/>
                <TextBlock Text="2. Нажмите 'ЗАКОНЧИТЬ РАБОЧИЙ ДЕНЬ' когда уходите с работы" 
                          TextWrapping="Wrap" Margin="0,2"/>
                <TextBlock Text="3. Система автоматически посчитает сколько времени вы отработали" 
                          TextWrapping="Wrap" Margin="0,2"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
